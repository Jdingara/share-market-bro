# NIFTY Options Auto-Trading Bot — Project Status

This file is the single source of truth for this project's goal, decisions, and current progress. Any AI assistant (Claude, Codex, or otherwise) or human picking this up should read this file first before making changes. Keep it updated as the project progresses — update the "Current Status" and "Open Decisions" sections whenever a phase completes or a new decision is made.

## Goal

Build an autonomous bot that trades NIFTY 50 index options (calls and puts) through Zerodha Kite Connect, for the account owner's personal use. The bot must analyze the market itself and decide when/what to trade — it is not a manual-signal tool.

## Core Trading Rules (decided, stable — do not change without explicit user request)

- **One trade per day, maximum.** The bot waits for its best setup, takes one entry, exits, then stops trading for the rest of that day — win or lose, no re-entry, no second attempt.
- **Exit bracket:** close the position at **+10% profit** (the goal) or **-5% loss** (the hard stop), whichever is hit first. (Changed from -10% to -5% on 2026-07-10 - see Current Status for the analysis behind this.)
- **Direction:** both calls and puts are in scope by design and **both remain live by default.** CALL has shown materially weaker precision than PUT in both backtest and live data (see Current Status) - a `--put-only` flag exists to skip CALL signals, but it is opt-in only (was briefly made the default on 2026-07-13, then explicitly reverted the same day per user instruction - see Current Status). The bot still decides bullish vs. bearish from its own analysis, and **sits out entirely** if no setup is clear ("no trade today" is a valid, expected outcome — never force a trade).
- **Underlying:** NIFTY 50 index options first; Bank Nifty is an explicit later addition, not in scope yet.
- **Position sizing:** NOT YET DECIDED — see Open Decisions below. Do not assume a specific model (fixed amount vs. daily-compounding reinvestment) without confirming with the user first.
- **Validation order, strictly in this sequence — do not skip ahead:**
  1. Backtest the strategy against historical data first, to measure a real historical win rate (never assume a strategy works without measuring it).
  2. Paper trading — simulated orders, real live market data, running on the user's own laptop during market hours (9:15am–3:30pm IST). No real money at risk.
  3. Live trading — only after paper trading has proven out over a meaningful sample of trading days.

## Broker & Account Setup

- **Broker:** Zerodha Kite Connect. Chosen over Angel One (genuinely free) and Upstox/Groww specifically because Zerodha has by far the most mature documentation and community for this kind of Python/algo-trading build — worth the small recurring cost.
- **Billing:** Kite Connect API access is credit-based: 500 credits ≈ ₹500 (inclusive of GST), valid for 30 days per app, auto-renewing because the user linked their Zerodha account for billing. The "Connect" plan tier is required (not "Personal" — Personal excludes historical data and live market quotes/WebSockets, which this project needs).
- **Zerodha app name:** `NiftyOptionsBot`. API key and Zerodha Client ID are stored only in `.env` (gitignored) - not written here on purpose, to keep this file safe to store in git/GitHub.
- **Credentials location:** `.env` in the project root (gitignored, never committed). Contains `KITE_API_KEY`, `KITE_API_SECRET`, `KITE_USER_ID`, `KITE_PASSWORD`, `KITE_TOTP_SECRET`. See `.env.example` for the template/field names.
- **TOTP 2FA is enabled** on the Zerodha account (required for automated daily login — Kite access tokens expire every day).
- **Credit renewal reminder:** the app's 500 credits were purchased 2026-07-07 and are valid ~30 days — expect to need renewal around **2026-08-06**, via the Billing page on developers.kite.trade.

## Non-Obvious Technical Findings (read before debugging auth issues)

1. **Kite Connect's login flow requires a one-time manual browser consent.** The first time a given API key + Zerodha account pair is used, Zerodha shows an "authorize this app" consent screen that a plain HTTP redirect chain cannot get past (tried `skip_session=true` — did not work). This had to be resolved by manually visiting `https://kite.zerodha.com/connect/login?api_key=<key>&v=3` in a real browser once, logging in, and clicking Authorize. After that one-time consent, the fully-automated `requests`-based login in `src/auth.py` works correctly with no browser involved. **If auth ever mysteriously breaks after being fine, check whether a NEW Kite Connect app was created (each new app needs its own one-time consent) before assuming the code is broken.**
2. **Kite Connect access tokens expire daily** — this is expected, not a bug. `src/auth.py` automates the login (password + TOTP) daily via `requests` calls to Zerodha's web login endpoints, since there is no officially documented headless login API. This is a widely-used community pattern, not part of Kite's official public API — if it starts failing, Zerodha's internal login endpoints may have changed shape.
3. **`pandas-ta` was deliberately dropped from dependencies** — it depends on `numba`, which does not support the Python 3.14 installed on the dev machine. EMA and RSI are simple enough to hand-implement directly in pandas; do not re-add `pandas-ta` or another indicator library without checking Python version compatibility first.
4. **SEBI's static-IP mandate (effective 2026-04-01)** only restricts *order placement* API calls, not login/data-fetching. It does not block anything in Phases 0–4 (auth, historical data, signal engine, backtesting, paper trading). It only becomes relevant at the live-trading phase (Phase 5), when real orders start being placed and a static IP must be registered with Zerodha.
5. **Index instruments (like NIFTY 50) show `volume: 0`** in historical data — this is normal, indices don't carry their own traded volume, only individual stocks/derivatives do.
6. **Expired option contracts are completely unavailable via Kite Connect** — confirmed directly by checking the live instrument list (earliest listed expiry is always today's date). There is no way to fetch real historical option premiums for backtesting; `src/options_pricing.py` simulates them via Black-Scholes instead. This is a real, permanent constraint of the API, not a bug to fix later.
7. **Transient network errors WILL happen during a 6+ hour unattended run and will kill the process if not handled** — confirmed by a real crash (`ConnectionResetError(10054)`) that silently killed `paper_trader.py` for nearly an hour before being noticed. `paper_trader.py` now retries network calls with backoff and has an outer catch-all that logs and continues rather than dying (see Current Status). If writing any other long-running unattended script (e.g. for Phase 5), apply the same pattern - don't assume a bare API call in a loop is safe unattended.
8. **RandomForestClassifier's `predict_proba` rarely gets close to 0 or 1 on noisy financial data** — on this project's data, max predicted confidence was only ~0.6-0.7 even on training data. Don't guess a confidence threshold (an initial guess of 0.6 turned out higher than the model's entire test-set output range, silently producing zero trades) - always inspect the actual predicted-probability distribution and training-set precision-at-threshold first.
9. **NSE changed NIFTY's weekly options expiry day from Thursday to Tuesday at some point before 2026-07-08** — confirmed live against Kite's instrument list, after a genuine live trading signal on 2026-07-08 failed to find a matching option contract (was computing expiry=Thursday 2026-07-16, which was never a real listed contract; real expiries were all Tuesdays: 2026-07-14, 07-21, 07-28...). **This wasted that day's one trade opportunity.** Fixed two ways: (1) `options_pricing.WEEKLY_EXPIRY_WEEKDAY` updated to Tuesday for backtesting/simulation (where no real instrument list exists for past dates, so a weekday guess is unavoidable); (2) live trading (`paper_trader.py`) no longer guesses a weekday at all — `option_lookup.find_nearest_valid_expiry()` now queries the actual live instrument list for the real nearest valid expiry, making it robust to NSE changing this convention again in the future without needing another code fix. All 3 ML models were retrained with the corrected assumption (see Phase 6 section - the comparison numbers shifted somewhat as a result, XGBoost remains the best performer).

## Full Roadmap

| Phase | Goal | Status |
|---|---|---|
| **0. Project setup & auth** | Python scaffold, Kite Connect credentials, automated daily TOTP login | ✅ Done, verified end-to-end with real account (2026-07-07) |
| **1. Historical data pipeline** | Pull & cache NIFTY 50 historical candles for backtesting | ✅ Done, verified — real daily candles pulled to `data/historical/` |
| **2. Signal engine (rule-based)** | EMA trend filter + Fibonacci retracement zones + RSI + candlestick confirmation, combined into one confluence rule producing a daily call/put/no-trade decision | ✅ Done, verified — see `src/indicators.py`, `src/signal_engine.py` |
| **3. Backtesting engine** | Replay the signal engine over historical data, simulate the +10%/-10% bracket, measure real win rate; tune thresholds until there's a genuine measured edge | ✅ Done, verified — see `src/options_pricing.py`, `src/backtester.py`. Result after fixing a 0DTE root cause: **+1.05%/trade, 63.6% win rate** over 18 trades (small sample, promising not proven — see below) |
| **4. Paper trading engine** | Real-time version: live data (REST polling, not WebSocket - see notes), same signal + bracket logic, simulated (not real) orders using REAL quoted premiums, full daily logging | ✅ Built and smoke-tested live — see `src/paper_trader.py`, `src/option_lookup.py`. Not yet run through a full trading day or multiple days |
| **5. Live trading** | Real Kite order API calls, safety guards (kill switch, daily loss circuit breaker), static-IP registration | ⬜ Not started |
| **6. ML enhancement** | Feature-engineer OHLCV + indicators, train classifiers (Random Forest, Logistic Regression, Gradient Boosting) on historical labeled outcomes, compare against the rule-based signal | ✅ Built, honestly 4-way compared, and **now the live default** — see below |

## Current Status (last updated: 2026-07-24)

Phases 0 through 4 and Phase 6 are complete. Phase 4 (paper trading) is built and smoke-tested but **not yet run through a full live trading day** - it now runs on the Phase 6 Gradient Boosting (XGBoost) signal by default (see the 2026-07-07 decision below), so tomorrow's run will be the first real test of that combination.

- **Phase 0/1**: `src/auth.py` (daily login) and `src/data_fetch.py` (historical candle fetch with correct per-interval pagination) verified live against the user's real Zerodha account.
- **Phase 2**: `src/indicators.py` (EMA, RSI, Fibonacci levels, 4 candlestick patterns) has 11 passing unit tests in `tests/test_indicators.py` against hand-computed/analytically-known values. `src/signal_engine.py` implements the confluence rule (`daily_trend_bias`, `fib_zones_for_today`, `generate_signal`) — see its module docstring for the important lookahead-bias contract callers must follow (`daily_df` = strictly prior days only, `intraday_df` = one day's candles only).
- **Phase 3**: Confirmed directly against the live Kite instrument list that **expired option contracts are not retrievable at all** (earliest listed expiry is always today) — so `src/options_pricing.py` simulates option premiums via Black-Scholes (historical/realized volatility standing in for implied vol) rather than replaying real historical option prices. 10 unit tests in `tests/test_options_pricing.py`, including an exact put-call-parity check. `src/backtester.py` replays `generate_signal` day-by-day over 132 cached trading days and simulates the +10%/-10% bracket via the pricing model.

  **Backtest result, initial (132 days, 2025-12 through 2026-07): 124 no-trade days, only 8 trades taken.** 2 hit +10%, 3 hit -10%, 3 closed at EOD. Win rate 40% (excl. EOD) / 25% (incl.). Average P&L -3.18%/trade.

  **Tuning round 1 — widened Fibonacci proximity (0.15%→0.3%) and relaxed candlestick confirmation to a 2-candle lookback window (`CANDLE_CONFIRM_LOOKBACK`):** trade count rose to 18, but win rate stayed at exactly 40% and P&L worsened slightly (-3.72%) — a consistent negative result across a bigger sample, not just noise. Diagnostic funnel analysis (`tests/diagnose_signal_funnel.py`) found the candlestick relaxation was a complete no-op (all 8 original trades' timing was unchanged) — the real bottleneck is the rarity of Fibonacci+RSI co-occurring at all (only 53 of ~1000 candles across the dataset), not candlestick pattern strictness.

  **Tuning round 2 — added Bollinger Bands** (`indicators.bollinger_bands`, window=20, 2 std-dev) as a second, independent "price at an extreme" trigger alongside Fibonacci, on the theory that a dynamic volatility-based zone might catch genuinely different setups. **Result: zero effect — all 18 trades still trace back to a Fibonacci zone (confirmed via the new `zone` field in `backtester.TradeResult`); Bollinger Bands never fired once in 132 days with these parameters.** Not a useful addition as configured.

  **Methodology fix — realistic intra-candle bracket checking:** the original backtester only checked the simulated premium at each 15-minute candle's *close*, letting some trades overshoot far past +/-10% (one hit -94.78%) before being caught, while other genuine intra-candle stop-outs were invisible if the candle's close happened to recover by the time it was checked. Fixed to check each candle's high/low range and exit AT the +10%/-10% threshold price the moment it's crossed (conservative assumption: if both target and stop were crossable within one candle, assume the stop hit first, since intra-candle order isn't knowable from OHLC data alone).

  **Root cause found — 0-2 days-to-expiry (0DTE) noise, not a bad entry signal:** checked days-to-expiry for each of the 18 trades and found every single one had 0-2 days left (median ~1 day), because `next_weekly_expiry()` always picked the *nearest* Thursday. Cross-checked underlying-index move vs. premium move per trade and found cases like the underlying moving +0.03% (favorably!) while the premium still swung -10% — a hallmark of extreme gamma/theta noise dominating near expiry, unrelated to whether the directional call was actually right.

  **Fix: `next_weekly_expiry()` now requires a minimum 3 days to expiry** (`MIN_DAYS_TO_EXPIRY` constant in `options_pricing.py`), rolling to the following week's expiry if the nearest one is too close. **Result after this single fix: win rate jumped to 63.6% (excl. EOD closes: 7 targets, 4 stops), and average P&L flipped positive: +1.05%/trade.** Individual trades now look sane (entry premiums ₹120-320, consistent with real time value; EOD closes range only -8.1% to +4.1%, no more wild overshoots). This is the current best result and a genuinely encouraging one — but from a still-small sample (18 trades, 11 of which are non-EOD), so treat as promising, not proven.

  **This means Phase 4 (paper trading) is no longer structurally blocked** — the core problem (0DTE noise swamping the signal) had a real, well-diagnosed fix, not just more threshold guessing.

- **Phase 4** is built: `src/paper_trader.py` runs the unmodified `generate_signal()` live during market hours via simple REST polling (not a WebSocket ticker - deliberate simplicity choice, more than sufficient for a one-trade-per-day strategy). `src/option_lookup.py` finds the real tradable option contract (confirmed NIFTY lot size is **65**, not 50) and fetches its **real quoted premium** via `kite.quote()` - no more Black-Scholes simulation from here on. No real orders are ever placed (`kite.place_order` is not called anywhere in this phase). 6 unit tests for the pure exit-decision logic (`tests/test_paper_trader.py`) and 4 for option lookup (`tests/test_option_lookup.py`).

  **Live smoke test succeeded** (2026-07-07, ~10:20am, 3-minute run): real login, real 65-day daily history fetch, real 34,860-instrument NFO dump, three clean 60-second polling cycles against real live data, correctly returned NO_TRADE each time (expected - early in the day, no confluence yet) with no errors.

  **Not yet done: a full trading day (or several) of paper trading to see a real entry/exit cycle happen live.** The smoke test proves the plumbing works, not that a live trade has actually been observed end-to-end yet.

**Next step: run the bot for one or more full trading days** (via the dashboard's Start button, or `py src/paper_trader.py` directly) to see real paper trades accumulate in `data/paper_trades/paper_trades.csv`, and compare that real performance against the backtest's +1.05%/trade expectation.

- **Dashboard added** (2026-07-07): `src/dashboard.py`, a local Streamlit web control panel — Start/Stop buttons for the paper trader (launched as a background subprocess, output redirected to `data/paper_trades/live_log.txt`), a live log viewer, and two report tabs (Backtest Results, Paper Trading Results) showing summary stats, an equity curve, and the full trade table, reading directly from the existing CSVs. Run via `py -m streamlit run src/dashboard.py`, opens at `http://localhost:8501`.
  - **Locked to localhost only** via `.streamlit/config.toml` — by default Streamlit also advertises a network/external URL, which would have made this reachable from outside the machine; since it can start/stop a bot tied to a real Zerodha account, that was closed off deliberately.
  - **Found and fixed a real bug during verification**: the background subprocess's output wasn't appearing in the live log for several seconds because Python buffers stdout differently when it's redirected to a file vs. a terminal. Fixed by launching with `-u` (unbuffered) so the log updates in real time, verified directly (log showed real progress within ~6 seconds after the fix, vs. staying empty before it).
  - Verified against real data: the dashboard's own summary computation matches `backtester.py`'s printed output exactly (18 trades, 63.6% win rate, +1.05% avg).

- **Real crash found and fixed during an actual live run** (2026-07-07, ~11:46am-12:40pm): a transient network blip (`requests.exceptions.ConnectionError` / `ConnectionResetError(10054)`) during a routine API call killed the entire `paper_trader.py` process with no recovery - it silently sat dead for nearly an hour before being noticed, having taken no trade. **Fixed in `paper_trader.py`**: added `_call_with_retry()` (retries any network call up to 5 times with a 5s backoff before giving up) wrapping every Kite API call in the loop, plus an outer try/except around the whole polling loop body that catches any remaining unexpected exception, logs it, pauses 30s, and keeps going rather than letting the process die. Verified with a fresh 2-minute live smoke test after the fix - runs clean. **This class of bug (unattended process dying silently on a transient error) is exactly the kind of thing to watch for once real money is involved (Phase 5) - worth extra scrutiny there, not just here.**

- **Phase 6 (ML signal engine, done ahead of the original sequence)**: built at the user's request, motivated by a sound observation — the rule-based engine only trades ~13.6% of days (18/132) because it needs one rigid, hand-picked pattern to align exactly, likely missing genuine opportunities that are "close but not exact." `src/ml_features.py` engineers 16 features per candle (RSI, Fibonacci/Bollinger distances, EMA trend ratio, momentum, realized vol, candlestick flags, time-of-day, day-of-week) - reuses the same indicator building blocks as the rule-based engine so the comparison is meaningful. `src/ml_signal.py` labels every historical candle by reusing `backtester.simulate_trade` (promoted from private to public specifically for this reuse) to simulate "would a CALL/PUT here have hit +10% before -10%," trains two conservative `RandomForestClassifier`s (shallow, min 10 samples/leaf - deliberately resistant to overfitting on ~130 days of data), and splits **chronologically** (train on first 70% of days, test on last 30%) rather than randomly, to avoid leaking correlated nearby days between train/test.

  **Confidence threshold had to be recalibrated, not guessed**: an initial arbitrary guess of 0.6 turned out to exceed the model's actual max output on the test period entirely (zero trades). Fixed by inspecting precision at various thresholds **using only the training set** (never peeking at test data to choose it) - 0.5 gives ~89% training precision vs. a 35-39% unconditional baseline, with a large enough sample (100+) to trust.

  **Extended same day (user's request): added Logistic Regression and Gradient Boosting (XGBoost) as two more model types**, trained and calibrated with the exact same rigor as Random Forest - `ml_signal.py` was generalized (`_build_estimator(model_type)`, one model per type) and threshold selection was formalized into `calibrate_threshold()` (scans training-set-only precision at each threshold, picks the lowest one clearing 70% precision with 30+ supporting samples) instead of eyeballed by hand per model. This also improved Random Forest's own result versus the first pass (which had used a manually-eyeballed 0.5 threshold instead of the formal 70%-precision rule).

  **Honest 4-way result on the held-out 40-day test period (2026-05-08 to 2026-07-06), identical bracket simulation for all** (first pass, before the expiry-weekday bug below was found and fixed):

  | Approach | Trades | Win/Loss/EOD | Win rate (excl. EOD) | Avg P&L |
  |---|---|---|---|---|
  | Rule-based | 7 | 3/0/4 | 100%* | +2.81% |
  | Random Forest | 29 | 19/10/0 | 65.5% | +3.10% |
  | Logistic Regression | 34 | 15/17/2 | 46.9% | **-0.63%** |
  | **Gradient Boosting (XGBoost)** | 36 | 27/8/1 | **77.1%** | **+5.45%** |

  *Rule-based's 100% is only 3 decided trades - too small to trust on its own.

  **Read honestly:** Logistic Regression (the simplest model, a straight linear boundary) came out **negative** - this is actually a valuable, deliberate sanity check, not a bad result: it shows the real relationship between these features and outcomes is **nonlinear**, which is why both tree-based approaches (Random Forest, XGBoost) outperform it - they're capturing real structure a straight line can't, not just adding complexity for its own sake. **Gradient Boosting is currently the strongest candidate by every metric** (most trades, highest win rate, best average P&L) - but it's also historically the model type most prone to overfitting small data if under-regularized, so this needs to be read as encouraging, not conclusive, until validated on more than one 40-day window (same caveat as always: candles within a day are correlated, so effective independent sample size is smaller than raw row counts suggest).

  **Re-run after fixing the expiry-weekday bug (see Non-Obvious Technical Finding #9) and retraining all 3 models with the corrected assumption:**

  | Approach | Trades | Win/Loss/EOD | Win rate (excl. EOD) | Avg P&L |
  |---|---|---|---|---|
  | Rule-based | 7 | 2/2/3 | 50.0% | -0.38% |
  | Random Forest | 36 | 21/14/1 | 60.0% | +2.08% |
  | Logistic Regression | 15 | 9/6/0 | 60.0% | +2.00% |
  | **Gradient Boosting (XGBoost)** | 38 | 24/14/0 | **63.2%** | **+2.63%** |

  Numbers shifted meaningfully once the simulation used the correct expiry day (makes sense - every simulated trade's time-to-expiry changed). **XGBoost remains the best performer on every metric**, though by a smaller margin than the first pass suggested, and Logistic Regression no longer looks as clearly bad - both good reminders that these numbers are sensitive to getting the underlying assumptions right, and that a single 40-day comparison shouldn't be over-read either way. The decision to run live with XGBoost (below) still stands on the corrected numbers.

  **Decision made (2026-07-07): `paper_trader.py` now defaults to Gradient Boosting (XGBoost)** for live paper trading, per the user's call after seeing the comparison. `paper_trader.py` gained a `--signal-source` flag (`rule_based`, `random_forest`, `logistic_regression`, or `gradient_boosting`, defaulting to `gradient_boosting`) via `_build_signal_fn()`, which wraps `ml_signal.generate_ml_signal` with the calibrated thresholds via `functools.partial` - matches `generate_signal`'s exact interface so the rest of the loop (entry, position-watching, exit, logging) is completely unchanged. `PaperTrade` now also logs which `signal_source` produced each trade. Smoke-tested after hours (market closed, so no live signal check, but confirmed model loading + startup wiring both work for `gradient_boosting` and `rule_based`). **First real market-hours run was 2026-07-08** - found the expiry-weekday bug (finding #9) when a genuine CALL signal fired but failed at the option-lookup step; fixed and verified against live data same day (see Current Status for the corrected comparison numbers). Compare via `tests/compare_ml_vs_rules.py` (loops over all `MODEL_TYPES`). Retrain all three via `py src/ml_signal.py`.

  **First-ever completed live paper trade (2026-07-08, 13:58-13:59): a genuine WIN.** After the expiry-bug fix, a real PUT signal (75% confidence) fired, found `NIFTY2671424200PE`, entered at Rs 265.25, and hit the +10% target within 2 minutes, exiting at Rs 298.20 (+12.42%). Full pipeline (signal -> option lookup -> entry -> monitoring -> exit -> logging -> correct one-trade-per-day stop) worked end-to-end for the first time.

  **Capital-based position sizing added same day** (`src/capital_manager.py`) - resolves the "position sizing" open decision from the very start of the project (see Open Decisions below for full detail). Starting capital Rs 20,000; the first real trade above correctly compounded it to **Rs 22,141.75**. `paper_trades.csv`'s existing row was retroactively updated with the new `lots`/`invested_amount`/`pnl_rupees`/`capital_after` columns for consistency, and `capital_state.json` was initialized to the post-trade balance so tomorrow's run picks up correctly rather than resetting.

  **`--max-trades-per-day` added to `paper_trader.py` (2026-07-08), default stays 1.** User's idea: rather than waiting 5-10 calendar days to gather 5-10 live trade results (one per day), temporarily raise this (e.g. to 20) to gather a real validation sample within a day or two, then switch back to 1 for the actual live-discipline track record. Implemented as a trade-slot counter (`trades_taken_today`) replacing the old boolean flag - each slot (successful trade, failed option lookup, or insufficient-capital skip) still counts against the limit and updates `capital` in-memory before the next slot, so multi-trade days compound correctly within the same run. Prints a visible warning when run with anything other than the default, as a reminder this is a validation setting, not the intended production behavior. Smoke-tested both modes after hours. **Important: raising this never lowers the bar for any individual trade** - every trade slot, whether the 1st or the 10th, independently has to clear the exact same calibrated confidence threshold. Nothing is ever forced just to fill slots.

  **Dashboard control added same day**: `render_bot_control()` in `dashboard.py` now has a "Max trades per day" number input (default 1) next to the Start button, passed through to `paper_trader.py --max-trades-per-day` when launched - so the next few validation days (agreed plan: run with a higher cap for ~2-3 days, review the win/loss sample, then drop back to 1) can be done entirely from the dashboard, matching the user's preference for the visual/GUI workflow over the command line.

  **Max capital per trade cap added same day (2026-07-08), user's own idea: "fix the max investment limit is 2 lakh."** Even though `capital_manager.py`'s compounding is otherwise uncapped (grows every win, shrinks every loss with no ceiling), the user wanted a hard ceiling on how much of that growing balance can ever be risked on a *single* trade - so a lucky streak that compounds the account to, say, ₹10 lakh doesn't suddenly mean one bad trade risks ₹10 lakh too. `deployable_capital(capital, max_per_trade=MAX_CAPITAL_PER_TRADE)` returns `min(capital, max_per_trade)` and is applied right before `calculate_affordable_lots()` in `paper_trader.py` - so lot sizing is always based on the capped amount, and any balance above ₹2,00,000 simply sits idle and untouched (verified: `apply_trade_pnl()` needed no changes at all, since it already only moves the invested portion). Wired through everywhere the trade-count cap was: a `--max-capital-per-trade` CLI flag on `paper_trader.py` (default ₹2,00,000), and a matching "Max capital per trade (Rs)" number input in the dashboard's Bot Control panel, both mirroring the existing `--max-trades-per-day` pattern. 3 new unit tests (`test_deployable_capital_*`) cover below-cap/above-cap/exactly-at-cap. Full suite now 53 tests, all passing.

**Real bug found and fixed on today's first 10-trade validation day (2026-07-09): rapid meaningless trades right at market close.** With `max_trades_per_day=10`, the loop kept accepting new entries all the way up to `MARKET_CLOSE_TIME` (3:30 PM), but any position opened after `FORCE_CLOSE_TIME` (3:25 PM) gets force-closed within the very next 15-second monitoring poll - it never gets a real chance to move toward +10%/-10%. This produced 6 near-instant back-to-back "trades" between 15:21-15:26, each measuring only a few seconds of random premium noise, inflating the trade count with results that don't reflect real signal performance. **Fixed**: the main loop in `paper_trader.py` now also stops looking for *new* entries once `now.time() >= FORCE_CLOSE_TIME`, even if trade slots remain unused - already-open positions are still force-closed exactly as before if still running at 3:25. Confirmed with the full test suite (53 passed).

**Agreed plan (2026-07-09): next 5 trading days (through ~2026-07-16) run with the trade-count cap effectively removed** (`--max-trades-per-day` set very high, e.g. 999, via the dashboard's now-uncapped "Max trades per day" input, `max_value` raised from 50 to 999) - so the real number of trades taken each day is whatever the ML confidence threshold (currently 33%) genuinely produces, not an artificial ceiling. Purpose: observe whether the confidence-based decision logic behaves sensibly under realistic, unrestricted conditions - zero trades on a quiet day is still a fully acceptable outcome, not a problem to fix. After this 5-day window, review the results together and decide: tune thresholds (see the CALL/PUT precision finding above), or move toward Phase 5 (real money) if the track record looks solid.

**Future improvement ideas discussed (2026-07-09), to revisit after the 5-day test window, not before:** user asked about DL/Gen AI/RAG/AGI as ways to push model confidence higher. Honest assessment given: deep learning (LSTM/Transformer) needs far more data than our ~130-200 trading days to avoid overfitting - tree-based models (current XGBoost) are actually the more appropriate choice at this data size; RAG and AGI aren't applicable to price prediction at all (RAG retrieves/summarizes text, doesn't forecast numeric outcomes; AGI isn't a real deployable tool). The one genuinely promising direction: **add new orthogonal data sources as extra ML features**, since the bot currently only reads NIFTY spot price + technical indicators and has zero visibility into:
  - **Options Greeks** (Delta, Gamma, Theta, Vega) - would let the model separate "real directional move" from "just time decay" or "just a volatility repricing," which current price-only features can't distinguish. Directly relevant to the 0DTE/gamma noise problem already solved once (see Finding #7-ish) - Greek-aware features could generalize that fix further.
  - **FII/DII daily net flow data** (published by NSE) - big foreign/domestic institutional buying or selling pressure, a widely-watched macro signal the bot has no access to today.
  - **India VIX** (NSE's volatility/fear gauge, derived from NIFTY options prices) - option premiums are heavily driven by implied volatility, not just direction; VIX spikes could explain some of the noisier trades already seen (e.g. today's rapid EOD sequence had real premium swings not obviously tied to underlying moves).
  User explicitly wants to start concentrating on these three (Greeks, FII/DII, VIX) for better tuning once the 5-day unrestricted test window concludes - not a new task to start now.

**Additional future-improvement ideas found 2026-07-13, same "revisit later" status:**
  - **Closing Range** `(close-low)/(high-low)` - a precise, volume-free 0-100% "who won this candle" score, easy to add as a numeric ML feature (no new data source needed, just OHLC already fetched).
  - **Inside bar / compression signal** - whether the current candle's range sits entirely inside the previous candle's range; a genuinely new signal *type* (coiling before a move) not captured by any of the current 16 features.
  - **Multi-candle momentum** - comparing how fast an impulsive move happened vs. how slow/fast its retracement is, as a trend-strength signal distinct from the existing `momentum`/`realized_vol` features.
  - **Multi-timeframe alignment** - checking a faster intraday timeframe (e.g. 5-minute) agrees with the current 15-minute signal before trading; came up independently across multiple sources.
  - ~~**Faster morning signals via 5-minute candles + model retrain.**~~ **Built 2026-07-15** - see Current Status below for full detail. Diagnosed 2026-07-13: the model can never generate a signal before ~1:15 PM on any day (confirmed across all 3 real trading days at the time: first trades at 13:58, 14:41, 13:16), because `RSI_PERIOD=14 + RSI_TURN_LOOKBACK=2` requires 16 completed candles before any feature can be computed - at 15-minute candles that's a hard 4-hour floor, regardless of how strong the morning's move looks. Originally deferred ("leave as-is, don't touch mid-test-week"), then explicitly requested to build 2026-07-15.

**5-minute early-session model built and live-verified, 2026-07-15.** A dedicated Gradient Boosting model trained specifically on 5-minute candles (`src/train_5min_model.py`, saved as model_type `gradient_boosting_5min`), used only until enough 15-minute candles exist for the primary model - cuts the earliest-possible-signal floor from ~4 hours after open to ~1h20m. Trained on 10,524 fetched 5-minute candles (140 days, 2025-12-17 to 2026-07-15) -> 7,560 labeled candles, calibrated thresholds call=0.44/put=0.46, held-out test precision CALL 45.8% (n=848) / PUT 57.9% (n=1,333) - both healthy sample sizes. `paper_trader.py` now checks candle count each poll: if the primary 15-min model doesn't have enough candles yet (`MIN_CANDLES_FOR_SIGNAL = RSI_PERIOD + RSI_TURN_LOOKBACK = 16`), it fetches 5-minute data instead and tries the early-session model; once enough 15-min candles exist, behavior is completely unchanged from before. Only wired for `gradient_boosting` (the live default) - other signal sources behave exactly as before, no early-session fallback attempted. **Live-verified the same day, not just backtested**: a 2-minute smoke test at 11:33 AM (well before the old ~1:15 PM floor) produced a genuine signal - `[early-session 5-min model] ML model confidence 0.61 for PUT` - and a real paper trade entry (`NIFTY2672124200PE @ 154.15`). 3 new tests added (`test_min_candles_for_signal_matches_rsi_warmup`, `test_no_early_session_model_for_non_gradient_boosting_sources`, `test_early_session_model_available_for_gradient_boosting`), full suite now 58 passing. No new dependencies required.

**Per-trade candlestick chart added (2026-07-10), user's request:** for every trade, wants to visually check whether a stop-loss exit would have recovered if held longer, or a target exit left further upside on the table. New module `src/trade_chart.py` (`generate_trade_chart()`) fetches the *traded option's own* 5-minute premium candles (not the underlying NIFTY index - the question is specifically about the option's price behavior) for a 2-hour window centered on entry time (1 hour before to 1 hour after - widened from an initial 30/30 same day per user request), marks the entry (blue up-triangle) and exit (colored down-triangle: green=TARGET, red=STOPLOSS, orange=EOD_CLOSE) via `mplfinance`, and saves a PNG to `data/paper_trades/charts/`. Added `matplotlib` and `mplfinance` to `requirements.txt` - both installed and imported cleanly on this machine's Python 3.14 (no repeat of the earlier pandas-ta/numba incompatibility). Wired into `paper_trader.py`'s trade-close flow, wrapped in try/except so a charting failure can never take down the trading loop (diagnostic nice-to-have, not core trading logic) - `PaperTrade` gained a `chart_path` field.

  **Dashboard integration**: rather than an always-visible image thumbnail, used Streamlit's built-in `st.column_config.ButtonColumn` (available since this project's streamlit version, 1.59.0) to add a "View Candle" button as the last column of the trade log table - clicking it displays the chart inline below the table via `st.image()`, driven by the button's `on_click` callback + `st.session_state` (per the `key` mechanism `ButtonColumn` provides to identify which row was clicked). `column_order` is used to hide the raw `chart_path` path text while keeping the button visible. Kept separate per-tab session-state keys (derived from the tab title) so Backtest Results and Paper Trading Results don't collide.

  **Backfilled all 25 existing trades same day** (2026-07-08 through 2026-07-10) with real, freshly-fetched charts - safe to do since none of the traded option contracts (all expiring 2026-07-14) had expired yet; this only works if charts are generated before expiry, since Kite Connect permanently loses historical data for expired contracts (Finding #6). **Important operational note for whoever runs this next**: this backfill was done manually, once, right after confirming the day's live `paper_trader.py` process had fully exited - rewriting `paper_trades.csv` (to add the `chart_path` column to old rows) while the live process might still be actively appending rows in the old column format would misalign the CSV. Going forward, every new trade gets its chart automatically at trade-close time, so this manual backfill should not be needed again unless the CSV schema changes again.

  **Real-world validation of the FORCE_CLOSE_TIME fix (from 2026-07-09) confirmed working**: today's live run (2026-07-10, uncapped `max_trades_per_day=999`, first day of the agreed 5-day test window) produced 21 trades total with normal-looking entries and exits, and only **one** clean EOD_CLOSE right at the very end (+5.70%) - no repeat of the previous day's rapid-fire meaningless-trade bug. Today's net result: capital moved from Rs 20,552.50 to Rs 19,395.50 (a loss day, part of the ongoing unrestricted test week, not yet reviewed/discussed with the user).

**Exit bracket changed from -10% to -5% stop-loss (2026-07-10), a core trading rule change, explicitly confirmed by the user after real evidence.** User's original idea was a *trailing/ratcheting* stop (raise the stop-loss as the price climbs, e.g. buy at 100, once it hits 110 move the stop to 105, "so we can never lose, only profit more if the trend continues"). Rather than guess, built a one-off simulation (`trailing_stop_sim.py` / `trailing_stop_sim2.py`, scratchpad only, not part of the codebase) that replays all 25 real trades' actual 1-minute option-premium candles (re-fetched from Kite - none of the contracts had expired) under several exit rules, using the candle's actual high/low at breach (not the exact threshold price) since the real bot's 15-second polling often catches an overshoot, same as the simulation should.

  **Results (same 25 real trades, real price paths):**
  | Rule | Total P&L |
  |---|---|
  | Current rule at the time (+10% / -10%) | +Rs 2,369 |
  | **+10% target / -5% stop, no trailing (adopted)** | **+Rs 5,339** |
  | Trailing, ratchet in 10% rungs (user's original idea) | -Rs 12,318 |
  | Trailing, tighter 5%-behind-peak ratchet | -Rs 12,662 |
  | Same tight -5% initial stop, but trailing instead of taking the fixed +10% target (cleanest isolated test) | -Rs 7,218 (vs. +Rs 5,339 for taking the target immediately) |

  **Why trailing underperformed, explained with a concrete example from our own data**: at entry Rs 176.15, price rose only modestly before reversing - the ratcheted stop only reached ~Rs 169 (still *below* the Rs 176.15 entry) before price fell through it, producing a real **loss of Rs 276**, not just "less profit" as the user initially assumed. The fixed rule, on the same trade, hit the clean +10% target first and banked +Rs 2,249. Two distinct mechanisms explain the gap: (1) a modest bounce can still leave the ratcheted stop below the entry price, so trailing can produce genuine losses, not just smaller wins; (2) even when trailing does stay above entry, it usually captures less than the fixed target would have, because it keeps waiting for "maybe more" that often doesn't come during a choppy 2-day stretch like this one. Trailing did clearly win on the one big trending trade in the sample (2026-07-08, Rs 150->450 move) - confirming trailing genuinely helps in a sustained trend, just not in the choppy conditions seen on most of these 25 trades.

  **Decision: adopted `STOP_LOSS_PCT = 0.05` in `paper_trader.py`** (was 0.10), target stays `TARGET_PCT = 0.10` unchanged - no trailing/ratcheting logic added, kept as the same simple fixed-bracket exit, just tightened. 2 new unit tests added (`test_stoploss_at_tightened_5pct_boundary`, `test_no_stoploss_just_above_5pct_boundary`), full suite now 55 tests passing. **Note**: `backtester.py` (used for the historical Phase 6 ML model comparison and `project_presentation.html`'s figures) still uses the original `STOP_LOSS_PCT = 0.10` - deliberately left unchanged since this decision was driven by live paper-trading evidence specifically, and touching the backtester would retroactively alter already-published/documented historical comparison numbers that weren't part of this decision.

  **User still wants to test the trailing/ratcheting stop idea live (not just simulated) at some point** - specifically the original -10%-initial/10%-rung version (buy 100 -> SL 90 -> price 110 -> SL to 100 -> price 120 -> SL to 110, etc.), since a single backtested comparison isn't the same as seeing it react to a fresh, real trading day. **Not built yet - deferred by the user's own choice**: "leave what we already had [the -5% fixed rule], we will change some on Monday after 4pm" (2026-07-10) - i.e., run Monday (2026-07-13) with the current fixed +10%/-5% rule unchanged, then revisit this experiment after Monday's market close. If picking this up: the trailing logic would need to be built as an opt-in mode (e.g. a new `--exit-mode` flag defaulting to the current fixed behavior) so the just-adopted -5% rule remains the default/production behavior on every day this isn't explicitly tested.

**CALL trades disabled (PUT-only), 2026-07-13 - the Monday-after-4pm review the above item deferred to.** Today (11 trades, all CALL, 3W/8L, -Rs 1,839.50) confirmed the CALL-model weakness first flagged from backtest data on 2026-07-09 was not just a historical artifact - it's showing up in real live results too. Computed batting-average/gain-loss stats for the full -5%-stop era (2026-07-10 through today, 24 decided trades): **CALL win rate 27.3% (3/11) vs. PUT win rate 53.8% (7/13)** - a stark, consistent gap. Overall era stats: 41.7% batting average against a 43.7% breakeven requirement (given the real 1.29:1 gain:loss ratio) - a modest but real **losing edge**, net -Rs 2,996.50 so far.

  **Considered raising the CALL threshold higher than PUT's, but the data argues against it**: the 2026-07-09 threshold scan (same CALL model still deployed) showed CALL precision does NOT climb with confidence the way PUT's does - it's flat around 33-42% from threshold 0.20 to 0.40, then actually *declines* at higher thresholds (32.9% at 0.50, 25.0% at 0.60, 16.7% at 0.70). Raising the CALL threshold on this same model would mean fewer CALL trades, not better ones - the confidence score doesn't reliably track real accuracy at any level tested. **Decision: disable CALL trades entirely (PUT-only) until the CALL model is retrained/recalibrated with more data**, rather than a threshold tweak that the evidence says won't help.

  **Implemented, then reverted the same day.** First shipped as `put_only: bool = True` (the new default, CLI `--allow-calls` to opt back in) - but the user explicitly overruled this a few hours later: *"no, dont skip call now... we need go with seller point of view. you need but call or put, Dont skip call now"* - both directions must stay live by default; the CALL-precision gap is a known weakness to keep investigating (see the Max Pain feature and the CALL-asymmetry explanation below), not a reason to stop trading that side. **Final/current implementation**: `put_only: bool = False` by default (both directions in scope, matching the Core Trading Rule above). `--put-only` CLI flag (opt-**in**, not opt-out) skips CALL signals only when explicitly passed - logged as `"CALL signal skipped (put_only mode): ..."`, doesn't consume a trade slot. Dashboard checkbox: "PUT only (skip CALL signals)", unchecked by default, with a warning if checked. No tests broke.

**Dashboard visual redesign (2026-07-15)**, per the user's request ("can we improve a little bit the UI... look like a very good application... why we are wearing a suit?"): full redesign of `dashboard.py` - custom theme (`.streamlit/config.toml` `[theme]` section), styled stat-tile cards (`stat_tile()`), and Altair charts replacing the previous plain tables (equity curve, win/loss outcome bar, daily P&L bar) - all rendered with `theme=None` so the custom color palette (`COLOR_GOOD`/`COLOR_CRITICAL`/`COLOR_MUTED`/`COLOR_BLUE`) is used instead of Streamlit's default Vega theme. Live-status badge added to Bot Control.

**Trade log improvements (2026-07-15), several rounds of user-driven correction:**
  - Added a formatted "P&L %" column next to the raw `pct_change` decimal.
  - Added per-day TOTAL subtotal rows - **not** a repeated "Day Total" column, which the user explicitly rejected ("Dont add new column, remove that. After every day add new total"). **Real bug caught by the user and fixed**: the first version summed `pct_change` naively across a day's trades, which is mathematically meaningless once trades have different position sizes and can even show the wrong sign vs. the real rupee outcome (user's screenshot: "here, 25% + in percentage of win, but total show - negative. how?" - naive sum showed +25.13% while the real weighted result was -0.27%, matching the actual negative rupee total). Fixed to a properly weighted `sum(pnl_rupees) / sum(invested_amount) * 100`.
  - Added a second per-day "NET (after costs)" row using real Zerodha F&O brokerage (Rs 20/executed order x 2 orders/trade) + Kite Connect's Rs 500/30-day API subscription amortized daily (Rs 16.67/day) - gross P&L alone overstates real profitability once real trading costs are counted (deliberately excludes STT/exchange charges/GST/stamp duty - a known incompleteness, not claimed to be exhaustive).
  - New "Daily Summary" tab (`render_daily_summary()`): full per-day breakdown (trade count, win/loss/EOD counts, win rate, weighted P&L%, gross P&L, brokerage+Kite cost, net P&L) plus overall totals.
  - **Date-range filter added to the Daily Summary tab (2026-07-15)**, per user request ("they need filter like this month, date to date wise, they can see that as report"): a "Period" radio (All time / This month / Custom range) above the per-day table and chart - Custom range shows two `st.date_input` pickers (From/To). Filters the trade data before the table, chart, and overall-totals tiles are computed, so the whole tab (not just the table) reflects the selected window. Implemented by filtering on a separately-parsed date series, not by mutating the `date` column itself, so the rest of the tab's string-based date handling stays unchanged. Verified directly against the real `paper_trades.csv` (63 real trades across 5 days) - "This month" correctly captured all 5 days, a custom 07-09→07-13 range correctly narrowed to exactly those 3 trading days.
  - **"Download as PDF" button added next to every table's native "Download as CSV" (2026-07-15)**, per user request ("everywhere the download csv there, give print pdf also"). New `dataframe_to_pdf_bytes(df, title)` renders a table to a landscape PDF page via `matplotlib`'s `ax.table()` (already a project dependency from the candlestick-chart feature - deliberately avoided adding `weasyprint`/`reportlab`, which would need extra system setup, same lesson as the earlier `pandas-ta`/`numba` incompatibility). Wired in via `st.download_button` right after each `st.dataframe()` call: the trade-log table in `render_report()` (used by both the Backtest and Paper Trading tabs) and the per-day breakdown table in `render_daily_summary()`. Verified two ways: (1) generated a real PDF from actual trade data directly, confirmed a valid `%PDF-` file (35KB) with no exceptions; (2) ran the full dashboard script headlessly via Streamlit's `AppTest` API (executes the real script end-to-end, catches runtime exceptions that a plain HTTP health-check would miss) - zero exceptions, all 3 tables rendered, all 3 PDF download buttons present and correctly labeled.

**Loss analysis, CALL asymmetry, and expiry-day check (2026-07-15):**
  - Found a first-6-trades-vs-rest-of-day pattern on a losing day (+Rs 7,315.75 on the first 6 trades vs. -Rs 6,633.25 on the rest) - part of the reasoning behind locking tomorrow's trade cap to 6 (see below). Re-ran the +20%/-5% bracket and the ratchet/trailing-stop simulations (same methodology as the 2026-07-10 stop-loss decision, see below) against that day's real trades specifically - both again performed worse than the current +10%/-5% fixed rule, consistent with the earlier findings. No rule change made.
  - **CALL-prediction asymmetry, explained** (user asked "why can't we predict CALL properly?"): NIFTY down-moves tend to be sharper/cleaner than up-moves (a real, well-known market asymmetry - fear moves faster than greed), so the CALL model's training examples are structurally noisier than PUT's, which is why its confidence score doesn't track real accuracy as reliably (matches the 2026-07-09 threshold-scan finding above - flat/declining CALL precision at higher thresholds). Not fixable with a threshold tweak - would need more training data or CALL-specific features.
  - **Expiry-day volatility hypothesis checked against real data, found NOT true**: user's idea was that Tuesday (NIFTY's weekly expiry day) sees unusually high fluctuation. Real historical daily ranges: Tuesday averaged 0.90% intraday range vs. Friday's 0.93%/0.57% - no meaningful expiry-day effect found. Also confirmed via code that live trading already has 0DTE protection (`find_nearest_valid_expiry(..., min_days=3)` default) regardless. User found the result "a bit confusing" and asked to leave it for now.
  - **"Zero to hero" cheap-options idea - discussed, explicitly deferred**: buying very cheap (Rs 1-10) far-OTM options hoping for a large payout (Rs 15-20) on a big move. Confirmed a real strategy pattern, but fundamentally different (much higher variance, near-certain total loss on most days) from the current strategy, not a small tweak. User: "we will plan for this decision making later." Nothing built.

**Tomorrow's plan locked in (2026-07-15), a firm commitment: run 2026-07-16 with `--max-trades-per-day 6`, no other trading-behavior changes.** User's exact words: "now, we gonna start tomorrow with maxi trade per day 6. will see tomorrow. no more changes." **Superseded the same morning (2026-07-16) by the split-session feature below, at the user's own explicit request** - this flat-6/day plan is no longer what actually ran; see below for what replaced it.

**Morning/afternoon split-session trading, built and wired 2026-07-16, replacing the flat-6/day plan above.** User's idea, prompted by noticing the early-session model (built 2026-07-15) meant "morning" trades were now possible for the first time: run a 6-trade quota starting ~10:35 AM (early-session 5-min model), pause once that's full, then open a FRESH 6-trade quota starting exactly at 1:15 PM (primary 15-min model) - up to 12 trades/day total, not 6.

  **Checked against real data before building anything**, per the project's evidence-first standard: segmented the 5 real trading days' actual logged trades by entry clock time (before/after 1:15 PM) and simulated what a 6-morning + 6-afternoon cap would have produced. **Important caveat found and flagged to the user**: on 4 of the 5 days (07-08, 07-09, 07-10, 07-13), there were **zero** trades before 1:15 PM, because the early-session model didn't exist yet on those days - so those days only test "capping total trades helps" (already well-established), not the actual morning+afternoon mechanism. **2026-07-15 is the only real test of the actual idea**: 12 capped trades would have made +Rs 4,975.75 vs. the 27 actual trades' +Rs 838.50 - encouraging, but a single day's sample, nowhere near proven. Told the user this plainly before building.

  **Implementation** (`paper_trader.py`): `SESSION_SPLIT_TIME = time(13, 15)` (same ~4h/16-candle floor as `MIN_CANDLES_FOR_SIGNAL`, expressed as a wall-clock cutover for quota purposes). New pure functions `_current_session(now)`, `_trade_slot_available(...)`, `_record_trade_slot(...)` (5 new unit tests) replace the single flat `trades_taken_today` check with two independent counters (`session_slots = {"morning": 0, "afternoon": 0}`) when `split_session=True`. Key behavior: if the morning quota fills before 1:15, the bot does **not** end the day - it pauses new entries and keeps polling until the clock crosses 1:15, at which point the (separate, still-empty) afternoon quota opens automatically - verified end-to-end with a simulated full-day clock sweep. New CLI flags `--split-session` (opt-in, default off) and `--max-trades-per-session N` (default 6); `--max-trades-per-day` is ignored when split-session is on. Dashboard: new "Split into morning/afternoon sessions" checkbox in Bot Control - when checked, swaps the "Max trades per day" input for "Trades per session (morning / afternoon)" and passes both new flags through to the subprocess. Verified via Streamlit's `AppTest` (checkbox toggle correctly swaps the input, no exceptions). Full suite now 66 tests passing.

  **Not yet proven, being live-tested today (2026-07-16) for the first time** - review after today's close same as any other real trading day, same evidence-first standard as everything else in this project.

**Split-session (6+6) dropped the same day, after seeing it live - replaced by a "stale-signal" fix + reverted to flat 6/day for a full week-long evaluation.** The morning session took its full 6-trade quota in 24 minutes (10:36-11:00 AM), all 5 losing trades on the SAME option contract (`NIFTY2672124100PE`), for -Rs 3,919.50 gross. Investigated with the real log and real 5-minute candles (not assumed): all 5 losing signals logged the exact same confidence (0.74 for PUT) - traced to the bot polling every 60s while the early-session model only gets new data every 5 minutes, so it could re-see an unrefreshed signal and re-enter within seconds of a stop-loss.

  **Built `_is_stale_signal()` in `paper_trader.py`**: blocks a new entry if its candle is the same as (or older than) the candle behind the last entry taken - forces at least one genuinely fresh candle between entries. 4 new unit tests, full suite now 70 passing.

  **Honestly validated against today's real candles before trusting it**: mapped each of the 5 losing entries to its actual last-completed 5-min candle (fetched fresh from Kite, not estimated) - only ONE of the five (10:47:45, sharing the 10:40 candle with the prior entry at 10:46:14) was a genuine stale-data duplicate; the other four were independently, validly re-signaled on fresh candles during a real ~25-minute NIFTY uptrend the model kept misreading as bearish. **So this fix recovers ~Rs 734.50 of the ~Rs 3,920 loss, not most of it** - the rest is the early-session model being wrong on real data during a real trend, a harder problem this fix does not solve. Told the user this plainly rather than overselling the fix.

  **Decision: drop the 6+6 split-session idea** (matches every other "more trades = worse" finding in this project - one real day of split-session data already contradicted it, no need to wait a week to confirm something this consistent). **Reverted to flat `--max-trades-per-day 6` (no split-session) starting today, with the stale-signal fix included, for a full week-long evaluation** - user's plan: run this combination for the whole next week and evaluate how the bot performs before any further changes.

**Two more real bugs found and fixed the same day, both from the CSV/dashboard side:**
  - **`paper_trades.csv` schema mismatch**: the header and all 64 pre-existing rows were still in the old 18-column format from before the Max Pain columns were added; today's live trades wrote 20-column rows, and pandas' C parser choked (`Expected 18 fields, saw 20`) when the dashboard tried to load it. Fixed by backfilling the two missing columns (`max_pain_strike`, `max_pain_agreed`) onto the header and all old rows as blank/NaN (old trades genuinely have no Max Pain data - the feature didn't exist yet), done carefully while the live bot's open position hadn't logged yet, verified clean afterward with both a raw line-count check and `pd.read_csv`.
  - **"View Candle" button (`dashboard.py`) could throw `IndexError` or silently show the wrong trade's chart**: `_handle_view_click`'s `df=display_trades` default argument was bound BEFORE the per-day TOTAL/NET summary-row grouping reassigned `display_trades` to a longer, differently-ordered table - so `click["row"]` (a position in the table actually shown to the user) was being used to index into an earlier, shorter, differently-laid-out version. Fixed by moving the handler's definition to after the grouping step, so it's bound to the exact same object passed to `st.dataframe()` right after - structurally guarantees the row position always matches. Verified via `AppTest` (no exceptions, all 3 tables + buttons still render) and the full test suite (70 passing).

**Critical operational bug found and fixed the same day: two `paper_trader.py` processes were running simultaneously (2026-07-16, ~12:18-12:43 PM), both trading against the same `capital_state.json` and `paper_trades.csv`.** This is the exact "duplicate process" risk flagged multiple times earlier in this project's history but never confirmed as an active problem until now. Symptoms: the live log looked garbled (interleaved output, a chart filename timestamp that didn't match its own log line). Confirmed directly via `Get-CimInstance Win32_Process` - two distinct PIDs, both launched minutes apart, identical `--max-trades-per-day 10` flags (not even matching the intended 6/day plan - leftover from an earlier restart).

  **Real consequence**: both processes independently read the same starting capital (Rs 18,534.25) and ran their own trade sequence against it, unaware of each other - producing two parallel, redundant copies of essentially the same real market movement (each simulating the same stop-loss, then the same target-hit, on the same contract). Only one process's final write survived in `capital_state.json`; the other's 2 trades were still sitting in the CSV as phantom entries that were never actually reflected in the real persisted capital.

  **Fixed**: stopped both processes, traced which of the two trade chains in the CSV actually matched `capital_state.json`'s value (Rs 18,891.75) by checking each row's implied starting capital against the previous row's `capital_after`, then removed the 2 phantom rows from the losing chain. Re-verified the full day's capital chain is now internally consistent end-to-end (every row's implied starting capital exactly matches the prior row's ending capital) and matches `capital_state.json` exactly. CSV field-count integrity and dashboard load both re-verified clean afterward.

  **Lesson for next time, and for whoever restarts this bot**: always confirm via `Get-CimInstance Win32_Process -Filter "Name = 'python.exe'"` (or equivalent) that exactly ONE `paper_trader.py` process is running before trusting the day's numbers - the dashboard's Start button does not prevent a second launch if a prior process wasn't actually stopped first (e.g., dashboard session state lost track of it, as documented in the README's known caveat about closing/restarting the dashboard while the bot runs).

**Dashboard performance fix (2026-07-17): "Refresh now" / auto-refresh had gotten noticeably slow (3+ seconds) as the trade log grew past 80 rows.** Root cause: `dataframe_to_pdf_bytes()` (added 2026-07-15 for the PDF download buttons) was called directly inline to build each `st.download_button`'s `data=` argument - meaning it ran on **every single script rerun** (any click, checkbox toggle, or refresh), not just when a user actually wanted to download. Since `st.tabs()` renders all 3 tabs' code on every rerun regardless of which is visually active, this meant rebuilding 3 full matplotlib-rendered PDFs on every interaction - confirmed directly: a single uncached call on the (now 80-row) trade table took **12.1 seconds**.

  **Fixed with `@st.cache_data` on `dataframe_to_pdf_bytes`** - Streamlit hashes the function's actual inputs (the table's content + title) and returns the cached result instantly if it's seen that exact data before, only re-rendering when the underlying trade data has genuinely changed. Verified directly: 2nd call with identical data = 0.01s (vs 12.1s uncached, ~1200x faster) and returns byte-identical output; 3rd call with deliberately changed data correctly took 7.7s and produced different output - confirms the cache invalidates correctly, not just "cached forever."

  **Also changed, per user's explicit request to discuss the UX before fixing**: "Auto-refresh every 10 seconds" now defaults to **on** (was off), and the separate manual "Refresh now" button was removed as redundant now that auto-refresh handles it and (thanks to the caching fix above) most of those automatic 10-second reruns cost almost nothing when no new trade has happened. Together this gives "stays current automatically, without feeling slow" - the user's stated goal - rather than either piece alone (auto-refresh without caching would have made the slowdown happen constantly instead of only on manual click).

  **Honest lesson-learned, logged as a reminder for future dashboard features**: when this PDF button was first built and verified (2026-07-15), the verification checked *correctness* (valid PDF, no exceptions, right content) but never checked *performance* or thought through how many times Streamlit would actually invoke it. Any future non-trivial computation placed directly in the render path needs the same question asked upfront: does this need to run on literally every rerun, or only when its actual inputs change / only when explicitly requested?

**Serious regression found and fixed the same day (2026-07-17): defaulting auto-refresh to on completely broke the dashboard - the Backtest/Paper Trading/Daily Summary tabs never rendered at all.** Root cause: the auto-refresh checkbox's `time.sleep(10); st.rerun()` lived *inside* `render_bot_control()`, which is called *before* the `st.tabs(...)` section in the main script. `st.rerun()` halts script execution immediately and restarts from the top - so with auto-refresh defaulting to True, every single page load hit that `st.rerun()` before ever reaching the tabs, meaning the tables were permanently stuck behind an infinite "render Bot Control, sleep 10s, restart" loop that never got further. This had nothing to do with Stop/Start state, even though the user first noticed it right after clicking Stop.

  **Fixed**: `render_bot_control()` now returns the checkbox's value instead of acting on it internally; the actual `time.sleep(10)`/`st.rerun()` call moved to the very end of the main script, after all 3 tabs have rendered. Verified two ways: confirmed via `grep` that the sleep/rerun code is now positioned after the tabs section (not just inline reasoning), and via `AppTest` with the checkbox forced through its normal code path - all 3 dataframes render, no exceptions, both Start/Stop buttons present. Full suite still 70 passing.

  **Lesson**: in Streamlit, `st.rerun()` must be the *last* thing a script does, full stop - anything placed after the code path that calls it will never run once that branch is taken. This wasn't caught before shipping the auto-refresh-on-by-default change earlier the same day, since the verification at that time (AppTest) hit an infinite-loop timeout and was worked around rather than treated as a signal something structural might be wrong - a good reminder that a test that can't complete is itself information, not just an inconvenience to route around.

**Separately confirmed the same day: the dashboard's Stop button did not actually stop the real running bot process.** A real `paper_trader.py` process (PID 18652, started 11:14 AM) was still running after the user clicked Stop - matches the already-documented caveat ("if you close/restart this dashboard while the bot is running, it loses track of that process"). `stop_bot()` only calls `.terminate()` on whatever process object is in `st.session_state.bot_process` for the *current* browser session - if that session lost track of the original process (e.g. the dashboard page was reloaded/reopened after the bot was started), Stop has nothing real to terminate, and the actual process keeps running invisibly. **Always verify independently** via `Get-CimInstance Win32_Process -Filter "Name = 'python.exe'"` (filtering for `paper_trader.py` in the command line) rather than trusting the dashboard's displayed running/not-running state alone - this is the second time today this exact class of issue has come up (see the earlier duplicate-process incident above).

**Max Pain / Open Interest shadow-mode analysis, built 2026-07-15.** The user's "mob seller/buyer" idea (large option-writer positioning pins price toward the strike that minimizes their combined payout as expiry nears) maps to a real, established options concept (Max Pain theory), and was confirmed technically buildable: Kite's `kite.quote()` returns real Open Interest per contract, and a single batched call handles the full ~186-contract chain for one expiry (confirmed live, 2026-07-15). Unlike the underlying index's own volume (permanently zero - Finding #5), OI is real and available - but it also isn't retained after expiry, so this can only ever be computed live, never backtested.

  `option_lookup.compute_max_pain_from_oi(oi_by_strike_type)` - pure function, finds the strike minimizing total combined CE+PE payout, 3 new unit tests. `option_lookup.compute_max_pain(kite, instruments, expiry)` - live wrapper that fetches quotes and calls the pure function.

  Wired into `paper_trader.py` as **shadow-mode logging only, per the agreed methodology - does NOT filter or change any trade.** Right after a real signal's option contract is found, computes that expiry's Max Pain strike and logs whether it agrees with the signal direction (PUT confirmed if spot > Max Pain, CALL confirmed if spot < Max Pain) - printed live and saved as two new `PaperTrade`/CSV fields, `max_pain_strike` and `max_pain_agreed` ("YES"/"NO", blank if the OI fetch failed - wrapped in try/except like `chart_path`, never blocks a trade). Live-verified same day against real data: Max Pain strike 24100 vs. real spot 24078.5 for the 2026-07-21 expiry. Full test suite now 61 passing.

  **Plan: let this accumulate silently for at least a week of real trades (starting 2026-07-16), then compare win rate/P&L for `max_pain_agreed=YES` vs. `NO` trades, and decide whether to promote it to an actual entry filter, adjust the mechanism, or drop it** - not a live filter yet, per the explicit "shadow mode first" agreement. **Not committed/pushed to git yet, deliberately** - user wants to see tomorrow's (2026-07-16) 6-trades/day run go cleanly first, then push afterward.

**2026-07-17: genuinely good day, +Rs 7,968.33 net (10 trades, 5W/5L exactly).** Confirmed the stale-signal guard held up fine, no repeat of the 07-16 whipsaw pileup. Ran with `max_trades_per_day=10` (not the planned 6) - a leftover dashboard setting, not a deliberate change; flagged to the user, no action taken since the result was good anyway. This is the day that ended up dominating the whole week's track record (see below).

**Critical recurring bug found (again) and fixed 2026-07-21: THREE `paper_trader.py` processes were running simultaneously, and tracing it back found this had ALSO silently corrupted 07-13 and 07-15 - not just today.** This is the third occurrence of the exact class of bug first found on 07-16 (see above) - the dashboard's Start button does not prevent duplicate launches, and nothing currently stops the same underlying issue from recurring day after day.

  **How it was caught this time**: the user asked to analyze a "stop trading once daily profit hits X%" idea using today's data, and two trades in the log (`NIFTY26JUL24200PE` entered at `10:44:10.440034` and `10:44:10.493626` - 53 milliseconds apart, same strike, nearly identical prices) were an obvious tell - the exact signature of two processes independently taking the same real-world trade.

  **Reconciliation methodology, generalized from the 07-16 fix into something more systematic**: for any day suspected of corruption, build a graph linking every row to its predecessor by matching `capital_after` (of a candidate predecessor) against `capital_after - pnl_rupees` (the implied starting capital) of each row, additionally requiring the candidate predecessor's `exit_time <= this row's entry_time` (a single real process can't have two open positions at once) to resolve ambiguous cases where multiple rows coincidentally shared the same capital value. This correctly untangled even 07-15's 27-row, 3-way-branching case (a naive single-predecessor-per-capital-value approach silently picked the WRONG branch there due to a dict-overwrite bug - caught by cross-checking against the *next* day's actual starting capital, which only matches one of the candidate chains).

  **Real chains recovered** (phantom rows removed, all verified against `capital_state.json` and the next day's actual starting capital):
  - **07-13**: real = 7 trades (was showing 11) - phantom chain of 4 removed.
  - **07-15**: real = 17 trades (was showing 27) - phantom chain of 10 removed.
  - **07-21**: real = 3 trades (was showing 10) - two phantom chains (7 rows total) removed.

  **Corrected daily net P&L (after real brokerage + Kite costs), all 9 real trading days so far:**

  | Date | Trades | W/L/EOD | Net P&L |
  |---|---|---|---|
  | 07-08 | 1 | 1/0/0 | +Rs 2,085.08 |
  | 07-09 | 10 | 2/2/6 | -Rs 2,005.92 |
  | 07-10 | 14 | 7/6/1 | -Rs 1,733.67 |
  | 07-13 | 7 | 2/5/0 | -Rs 1,125.42 |
  | 07-15 | 17 | 8/8/1 | +Rs 1,883.83 |
  | 07-16 | 16 | 5/10/1 | -Rs 2,236.17 |
  | 07-17 | 10 | 5/5/0 | +Rs 7,968.33 |
  | 07-20 | 9 | 3/5/1 | +Rs 1,579.83 |
  | 07-21 | 3 | 1/2/0 | -Rs 1,400.92 |
  | **Total** | | | **+Rs 5,014.97** |

  **Corrected overall CALL vs PUT** (previously reported numbers were partly inflated by phantom duplicate losing trades, particularly from 07-13): **CALL 38.5% win rate (10W/16L, 28 trades)**, **PUT 47.1% (24W/27L, 59 trades)**. CALL is still weaker than PUT, but the gap is meaningfully smaller than the 27.8%-vs-46.2% figures reported earlier this week - those were partly an artifact of this bug, not a true reflection of the model.

  **Sobering honest read of the corrected totals**: the entire +Rs 5,014.97 net track record depends almost entirely on ONE day (07-17, +Rs 7,968.33). Without that single day, the other 8 days combined are **-Rs 2,953.36** - a net loss. This is not yet evidence of a repeatable edge; it's one good day carrying an otherwise flat-to-negative record. Told to the user plainly.

  **Still unresolved, needs a real fix before this can be trusted again**: nothing currently prevents a second `paper_trader.py` launch. This has now happened 3 times (07-16, and twice more discovered together on 07-21 once traced back). A lock file (e.g. write a PID file on startup, refuse to start if one already exists and that PID is alive) would structurally prevent this rather than relying on manually remembering to check `Get-CimInstance Win32_Process` before every start - not yet built, should be prioritized before the next live-money conversation.

**Daily profit-target / "kill switch" idea discussed and tested 2026-07-21 (user's idea), using the now-corrected data.** Proposal: once cumulative P&L for the day reaches +N% of that day's starting capital, stop taking new trades for the rest of the day. Tested N=5/10/15/20% against all 9 real days:

  | Stop-at | Total (9 days) |
  |---|---|
  | Actual (no daily cap) | +Rs 8,645.00 (gross) |
  | +5%/day | +Rs 7,159.75 |
  | **+10%/day** | **+Rs 8,466.25** |
  | +15%/day | +Rs 5,161.00 |
  | +20%/day | +Rs 7,114.25 |

  **+10% specifically is nearly a wash** (-Rs 178.75 vs actual, statistically noise over 9 days) - not the clearly-worse result the 15%/20% versions showed. Per-day mechanics: **helped** on 07-10 (+Rs 3,305.25 - a real case of "recover from an early loss, cross +10%, then would have avoided two big subsequent losses that turned the day negative anyway") and 07-15 (+Rs 321.75); **hurt** on 07-17 (-Rs 3,805.75, cutting the week's best day nearly in half since the bot kept genuinely winning past the 10% mark); no effect on the other 6 days (never reached +10% that day either way).

  **Decision: NOT adopted for now.** Genuinely a coin-flip on this sample (helps on peak-then-reverse days like 07-10, hurts on sustained-trend days like 07-17) - not proven good or bad, and adopting it now would also contaminate the still-ongoing "observe the current setup for a week, unchanged" evaluation, the same reasoning that led to reverting the split-session idea earlier this week. **Tomorrow (2026-07-22) runs identically to today's cleaned-up setup, no kill-switch, no other changes** - revisit this specific idea as its own deliberate test once the observation week concludes and more days of clean data exist.

**Real incident, 2026-07-23: the bot's Kite access token became invalid mid-session (not at the expected daily reset), leaving a real open position unmonitored for ~40 minutes with no way to self-recover.** Two trades completed normally (11:27-12:14), then a third entered at 12:14:25 (PUT `NIFTY26JUL24000PE` @ 163.95) - starting at 12:28:41, every subsequent API call failed with `TokenException('Incorrect api_key or access_token.')`, repeating every ~30s for the next 40 minutes with the position still nominally "open" and un-monitored. **This is a different failure mode than the already-documented daily token expiry** (Finding #2) - the token had been working fine for over an hour into the session, then broke mid-day, most likely because something else (a manual browser login, another script) used the same credentials and invalidated the session token Kite had issued this process.

  **Root cause confirmed, not just guessed**: a fresh `auth.login(force=True)` call succeeded immediately when tested, proving the account/API subscription was fine - only that one process's specific token had gone stale.

  **The real gap this exposes**: `_call_with_retry()` only retries `requests.exceptions.RequestException` (transient network errors); a `TokenException` isn't caught there and falls through to the outer catch-all, which just sleeps and retries the *same* already-broken `kite` object forever, with no way to distinguish "this will resolve itself" from "this will never resolve without a fresh login." **Not yet fixed** - worth adding: catch `TokenException` specifically and re-run `auth.login(force=True)` to get a fresh token before continuing, rather than retrying blindly.

  **The stuck position was reconstructed by hand using real historical data, not left as a silent gap**: fetched real 1-minute candles for the exact contract from Kite (once a fresh login confirmed working) and found the premium first crossed the +10% target threshold (163.95 -> 180.34) at the 12:29:00 candle (high 181.95). Logged using the target threshold price itself (Rs 180.35) as a conservative exit, matching the project's established intra-candle-crossing convention from the backtester. Verified the full capital chain end-to-end afterward - fully consistent. The stuck process was then stopped.

  **2026-07-23 closed at +Rs 3,016.00 (+10.53%), 3 trades (1 STOPLOSS, 2 TARGET, one of the wins being the reconstructed trade above) - capital Rs 28,645.00 -> Rs 31,661.00.** User's call: leave the bot stopped for the rest of the day rather than restart after the token incident (discretionary, one-off decision given the day was already irregular - explicitly NOT treated as validating the daily-profit-target idea, which remains "not adopted" per the analysis above). Resume normally 2026-07-24.

**2026-07-24: early-session (5-min) model's confidence threshold formally scanned for the first time (previously only done for the primary 15-min model, on 2026-07-09).** Prompted by a real observation: 8 consecutive trades that morning all fired at the *exact same* confidence (0.68 for PUT), which raised the question of whether raising the threshold would help. Ran the identical held-out-test-set methodology used on 07-09, but specifically against `gradient_boosting_5min`'s actual test split (2,268 candles, 42 days never used in training):

  - **PUT is essentially flat across the whole threshold range** (56-58% win rate from 0.30 to 0.80) - unlike the primary model's PUT, which *does* improve with higher confidence. Raising the threshold would not have filtered out that morning's losers while keeping the one winner (all fired at 0.68) - it would just mean fewer trades at the same odds. **Confirms PUT's win rate (55-58%) is comfortably above the ~33% breakeven this bracket needs, so that rough stretch was normal variance around a real edge, not evidence the edge is broken.**
  - **CALL collapses at high thresholds** (32.3% at 0.60, 14.3% at 0.65) - the *same* pattern already found for the primary model on 07-09. Two independent models now confirm this is a structural CALL weakness, not one bad stretch or one model's training quirk.

**Max Pain shadow-mode data evaluated for the first time (50 trades with real data since 2026-07-15), and it's the opposite of the original theory.** Trades where `max_pain_agreed=YES` did *worse* (23.1% win rate, -Rs 815.75 total) than trades where it *disagreed* (42.9%, +Rs 5,850.00 total) - consistent for both CALL (33.3% vs 46.7%) and PUT (20.0% vs 40.0%) separately. **Max Pain as originally conceived ("require agreement with the signal") is not supported by the data - do not build it as a live filter in that direction.** Sample is still modest (50 trades), so this isn't the final word, but it's a consistent pattern across both directions, not noise in one bucket.

**Three concrete changes made 2026-07-24, based on the above plus everything found this week:**

1. **PUT-only is now the default** (`put_only: bool = True` in `paper_trader.py`, was `False`). CLI flag inverted to `--allow-calls` (opt-in, was `--put-only` opt-in) to match. Dashboard checkbox renamed "Allow CALL trades" (unchecked by default, was "PUT only" checked-to-enable). This is a *repeat* of the 07-13 decision that was reverted the same day - but this time backed by two independent models both showing CALL's confidence collapses at high thresholds, not just one rough week.

2. **Lock-file guard added to prevent duplicate `paper_trader.py` launches** - the bug that corrupted data 3 separate times this week (07-16, 07-13, 07-15, 07-21). `_acquire_lock()`/`_release_lock()` in `paper_trader.py`: writes the PID to `data/paper_trades/paper_trader.lock` on start, refuses to start if that file already names a PID that's still alive (checked via Windows' built-in `tasklist`, no new dependency), and cleans up the lock in a `finally` block on exit. A stale lock (dead PID, e.g. from a crash or force-kill) is correctly detected and doesn't block a legitimate restart - verified both via 6 new unit tests AND a real live test (an interrupted smoke test left a stale lock, and the next real run correctly detected it was dead and proceeded). `run()` is now a thin wrapper around the renamed `_run_impl()` that handles the lock; the CLI/dashboard entry points are unchanged.

3. **Automatic re-login on `TokenException`** - the gap found necessary by the 07-23 incident (a mid-session token invalidation left a real position unmonitored for ~40 minutes because the bot just retried the same broken `kite` object forever). The main loop now catches `TokenException` specifically and calls `auth.login(force=True)` for a fresh token before continuing, rather than falling into the generic catch-all's blind retry.

  All three verified: full test suite 76 passing (6 new lock-file tests), dashboard AppTest clean, and a real live smoke test confirming the lock acquires/releases correctly and the new PUT-only messaging displays as expected.

## Open Decisions (resolve before relevant phase starts)

- ~~Handle `TokenException` with an automatic re-login, not blind retry~~ **Resolved 2026-07-24** - see Current Status.

- ~~Prevent duplicate `paper_trader.py` launches with a lock file~~ **Resolved 2026-07-24** - see Current Status.
- **Daily profit-target ("kill switch") idea - not adopted, revisit after the observation week with more data.** +10%/day tested as a near-wash on 9 days (helps on peak-then-reverse days, hurts on sustained-trend days) - genuinely inconclusive on this sample size, not dropped for cause. Re-test once more clean days accumulate.
- **Future plan: multi-instrument support (NIFTY + Bank Nifty + FinNifty + others, running together) - discussed 2026-07-11/12, explicitly deferred, nothing built yet.** User's ultimate goal: the bot should eventually analyze **multiple indices at once** and be able to take a genuine trade on more than one simultaneously (not just NIFTY 50 alone), with a single dashboard control to pick which ones are active that day (checkboxes) and one Start button. Full discussion, to revisit when this becomes the active priority:

  - **Which instruments are actually viable** (all confirmed technically possible, none built): Bank Nifty and FinNifty (Nifty Financial Services) are the natural next two: both NSE weekly-expiry index options like NIFTY. **MIDCPNIFTY** (Nifty Midcap Select) is a third real NSE weekly-index-options candidate. **SENSEX/BANKEX** weekly options are also possible but trade on the **BSE**, not NSE - would need to confirm Kite gives the same kind of data/access there before assuming parity. **Individual stock options** (100+ NSE F&O stocks) are a fundamentally bigger, different category - monthly (not weekly) expiries, thinner liquidity, very different volatility per stock - not a small extension like the indices.
  - **Every new instrument needs its own full validation cycle** - same discipline already established for NIFTY, not a shortcut: its own lot size (confirmed live via Kite's instrument dump, never guessed - exactly the lesson already learned once with NIFTY's lot size), its own expiry-day schedule (the existing `find_nearest_valid_expiry()` mechanism already handles this safely by querying live data, so this part reuses cleanly), its own historical data pull, its own backtest, and its own trained ML models (NIFTY-trained models should NOT be reused on a different index - different volatility profile means the models won't transfer well, confirmed by reasoning already applied when Bank Nifty was first discussed).
  - **Real risk worth remembering**: NIFTY and Bank Nifty (and likely FinNifty) tend to move together, since Bank Nifty is a large weight within NIFTY 50. Running multiple of these at once isn't automatically "more diversification" - on many days it'll effectively be the same directional bet, just larger in total size. Worth reconsidering position sizing/risk limits with this in mind when the time comes.
  - **Architecture decision (recommended, not yet built): one separate bot process per instrument**, not one complex process juggling multiple concurrent positions. Reuses almost all of the existing `paper_trader.py`/`capital_manager.py`/`ml_signal.py` code unchanged (just parameterize the underlying symbol, lot size, and model type instead of hardcoding "NIFTY 50" everywhere it currently appears) - much lower engineering risk than a rewrite, and if one instrument's bot has an issue it doesn't affect the others (full isolation).
  - **UI decision (recommended, not yet built): keep ONE dashboard, one URL, one command** (`py -m streamlit run src/dashboard.py`) rather than a separate dashboard/URL per instrument - a checkbox per instrument plus one Start button, each checkbox still launching its own separate, isolated background subprocess underneath. Much simpler day-to-day than juggling multiple browser tabs/terminal windows. Confirmed one laptop can comfortably run several of these lightweight bot processes at once (mostly idle, periodic polling, not CPU-heavy) - the real constraint to watch for is Kite's **API rate limits** if multiple bots hit the same account simultaneously, not yet checked against Zerodha's docs.
  - **Fund allocation - the trickiest part, with a clear recommended remedy**: right now there is ONE shared `capital_state.json`. Running multiple bots concurrently against that same shared file risks a real race condition (two processes reading/writing the same file near-simultaneously can corrupt it or silently lose an update) - not just a design question but a genuine correctness bug waiting to happen. **The recommended fix solves both the race condition and the "how is capital split" question at once: give each instrument its own separate capital file** (e.g. `capital_state_nifty.json`, `capital_state_banknifty.json`, ...) **with its own explicit, user-chosen starting allocation** (an input box per instrument next to its checkbox on the dashboard, e.g. "₹20,000 to NIFTY, ₹15,000 to Bank Nifty") rather than one shared/contested pool. Side benefit: each instrument naturally gets its own separate trade-log CSV too, keeping performance comparisons clean per instrument. Honest tradeoff accepted: capital sitting idle in one instrument's pool can't automatically flow to another instrument having a better day - a deliberate, safer simplicity choice over a dynamically shared pool.
  - **Status: purely a plan for now.** User's explicit instruction (2026-07-12): "we will plan for this later... but remember all as future plan." Nothing to build until this is picked back up.

- ~~Should a signal replace the rule-based one in paper trading, and which one?~~ **Resolved 2026-07-07**: paper trading now defaults to Gradient Boosting (XGBoost). Still open: how many real trading days of live paper trading are "enough" before trusting this for Phase 5 (see below) - this decision was made from one 40-day backtest window, so the live track record matters more than usual here.
- **XGBoost threshold tuning - deferred, not urgent (raised 2026-07-09).** During today's live run (multi-trade validation day, no trade fired for several hours), analyzed real win rate at different confidence thresholds on the 40-day held-out test set: PUT is well-calibrated (win rate rises cleanly from 56.6% at 0.20 to ~60-63% at 0.40-0.70, more confidence = more reliable), but **CALL is not** - win rate stays flat around 33-42% regardless of threshold and never climbs the way it should, meaning the CALL model's calibrated 70%-training-precision target didn't generalize to the test period. Neither side reliably crosses 70% win rate at a trustworthy sample size (only tiny, noisy samples do, e.g. n=2-6). A small nudge (33%->35%) tested and found to change almost nothing (CALL 40.8%->39.6%, PUT 58.1%->60.4%) - not worth doing on its own. **User's call: leave thresholds untouched for now, keep watching live results one more day (2026-07-10), and only revisit tuning - likely CALL-specific (e.g. a higher/separate threshold, or recalibrating with more data) - in a calm review session, not reactively mid-trading-day.** Also confirmed same day: when both CALL and PUT clear 33% simultaneously (happened twice today, ~13:15 and 13:30), the bot correctly treats it as ambiguous and skips - user explicitly chose to keep this conservative behavior rather than auto-picking the stronger side.

- ~~Position sizing / capital compounding~~ **Resolved 2026-07-08**: `src/capital_manager.py` (new module) tracks a running paper-trading balance, starting at **₹20,000**, persisted across days in `data/paper_trades/capital_state.json` (not reset each morning). Before every trade, `calculate_affordable_lots()` works out how many whole lots (lot size confirmed live: **65**) the current balance can afford at that day's real premium, rounding down - if that's 0, the bot logs why and sits out for the day rather than forcing a fractional position. `apply_trade_pnl()` updates the balance after each trade closes (only the invested portion moves; idle leftover capital that wasn't enough for one more lot is untouched). This compounds up on wins and down on losses, exactly as discussed - not a one-way ratchet. 8 unit tests, including a hand-verified match against the actual real trade from earlier today (₹20,000 → ₹22,141.75 on the +12.42% win). `paper_trader.py` and `dashboard.py` both updated to show real rupee P&L and lot counts, not just percentages.
- ~~Max capital risked per single trade as the balance compounds up~~ **Resolved 2026-07-08**: hard-capped at **₹2,00,000 (2 lakh)** per the user's explicit instruction. See Current Status above for implementation detail (`deployable_capital()`).
- **How many days/weeks of live paper trading is "enough" before considering Phase 5 (live trading)?** Partially resolved 2026-07-18: **one more week of paper-trading observation (starting 2026-07-18), then decide on next steps** - user's plan, explicitly agreed as conditional, not a foregone conclusion ("we wait for the next week observation. then we plan something"). What that decision should hinge on, per this discussion: (1) whether CALL's win rate (still 27.8% overall as of 2026-07-17) actually improves or keeps dragging results down, (2) whether the 2026-07-16 stale-signal-guard fix holds up across multiple days, not just the one good day seen so far (07-17: +Rs 7,968.33 net, 10 trades, 5W/5L - encouraging, but a single data point), (3) a win rate that clears breakeven consistently, not just on a lucky stretch. **Explicitly flagged to the user: "go for real money" is not a settings toggle - Phase 5 has literally no code written yet.** No `kite.place_order` call exists anywhere in the codebase; every trade so far has been simulated bookkeeping against real quotes. Before real money, still needs: real order placement wired in, a kill switch, a daily loss circuit breaker, and SEBI's static-IP registration with Zerodha (a compliance requirement specifically for live order placement). Do not skip ahead to building any of this without an explicit new decision from the user after the observation week concludes.
- ~~Max Pain / OI shadow-mode review~~ **Resolved 2026-07-24** - see Current Status. Data does not support the original "require agreement" theory; not built as a live filter. Could revisit an *inverted* filter (favor disagreement) once more data accumulates, but not attempted yet - purely observational at this point.
- ~~Confidence threshold re-scan~~ **Resolved 2026-07-24 for the early-session model** - see Current Status (PUT flat, not worth raising; CALL collapses, addressed via PUT-only default instead of a threshold tweak). The *primary* 15-min model's PUT threshold (currently 0.33) still hasn't been re-scanned against current data - the 07-09 backtest evidence suggested raising it could help, but that was never re-verified with the extra weeks of data since. Worth doing if the primary model becomes more heavily used again.
- **"Zero to hero" cheap-options strategy (buy Rs 1-10 options for a Rs 15-20 payout on a big move)** - raised 2026-07-15, explicitly deferred by the user to a future planning session. Nothing built; treat as a fundamentally different, much higher-variance strategy if picked up later, not a small tweak to the current one.

## Repo Structure

```
Share_Market_Bro/
  PROJECT_STATUS.md     # this file - read first
  README.md             # setup/usage instructions
  .env                   # real credentials (gitignored, never commit)
  .env.example           # credential template
  requirements.txt
  src/
    auth.py              # daily Kite Connect login (TOTP-based)
    data_fetch.py         # historical candle fetcher
    indicators.py         # EMA, RSI, Fibonacci levels, candlestick patterns
    signal_engine.py       # confluence rule -> daily CALL/PUT/NO_TRADE decision
    options_pricing.py     # Black-Scholes premium simulation (no real historical option data available)
    backtester.py           # day-by-day replay + bracket simulation + reporting
    option_lookup.py         # finds real option contracts + real quoted premiums (Phase 4+); also compute_max_pain() (shadow mode)
    paper_trader.py          # live paper trading loop (Phase 4) - simulated orders, real quotes
    dashboard.py             # local web control panel + reports (Streamlit) - Backtest / Paper Trading / Daily Summary tabs
    ml_features.py            # feature engineering for the ML signal engine (Phase 6)
    ml_signal.py               # labeling, training (3 model types), generate_ml_signal (Phase 6)
    train_5min_model.py         # trains the early-session (5-min candle) gradient_boosting model
    capital_manager.py          # paper-trading capital tracking + lot-size position sizing
    trade_chart.py               # per-trade candlestick chart generator (option premium, 2hr window)
  .streamlit/config.toml     # locks the dashboard to localhost only - do not remove
  tests/
    test_indicators.py                # automated unit tests (pytest)
    test_options_pricing.py            # automated unit tests (pytest), incl. put-call parity check
    test_option_lookup.py               # automated unit tests (pytest)
    test_paper_trader.py                # automated unit tests (pytest) for exit-decision logic
    test_ml_features.py                  # automated unit tests (pytest)
    test_capital_manager.py               # automated unit tests (pytest)
    manual_signal_sanity_check.py     # manual eyeball check against real data, not automated
    diagnose_signal_funnel.py          # one-off diagnostic: which confluence condition is the bottleneck
    compare_ml_vs_rules.py              # honest side-by-side backtest comparison, not automated
  data/
    historical/            # cached candle CSVs
    backtest_results/       # trades.csv from the last backtester.py run, plus *_test_period.csv comparisons
    paper_trades/           # paper_trades.csv (real trade log incl. lots/rupee P&L), capital_state.json (running balance)
    models/                 # trained ML models per type: call_model_<model_type>.joblib, put_model_<model_type>.joblib,
                             #   thresholds_<model_type>.joblib - gitignored, derived artifacts (model_type = random_forest /
                             #   logistic_regression / gradient_boosting)
```
